import { createClient } from 'microcms-js-sdk'; //ES6

// Initialize Client SDK.
const client = createClient({
  serviceDomain: 'iris', // YOUR_DOMAIN is the XXXX part of XXXX.microcms.io
  apiKey: 'fdde1c944a854e1393a74dc0c8f7da8a26c3',
});
